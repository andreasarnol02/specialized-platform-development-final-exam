{
  "name": "my-skill-workspace",
  "private": true,
  "engines": {
    "node": ">=22.22.0"
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names api,web --prefix-colors blue,green \"npm --prefix api run dev\" \"npm --prefix web run dev\"",
    "mobile": "npm --prefix mobile start",
    "seed": "npm --prefix api run seed",
    "test": "npm --prefix api test -- --runInBand && npm --prefix web test -- --runInBand",
    "build": "npm --prefix web run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
